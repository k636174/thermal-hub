# Agent Guide

## 開発原則

本プロジェクトは仕様駆動開発を採用する。機能追加・変更・不具合修正では、実装より先に `docs/specification.md` または `docs/specs/` 配下の仕様を更新し、期待動作と受入条件を明文化する。

## 実装フロー

1. 関連仕様と既存コードを確認する。
2. 仕様書を追加または更新する。
3. 仕様に対応するテストを作成または更新する。
4. CakePHPの規約に従って実装する。
5. `composer check` を実行する。
6. READMEと運用手順に影響があれば更新する。

## セキュリティ規約

- 全業務データの読み書きでログインユーザーの `user_id` を条件に含める。
- パスワード、秘密情報、プリンターへの送信内容をログへ平文出力しない。
- Entity/Tableのvalidationとサーバー側の所有者検証を行う。
- ESC/POS送信先は登録済みプリンターに限定する。

## コーディング規約

- PHP 8.2以上、`strict_types=1`、CakePHP 5.4の規約を使用する。
- DB変更はmigrationで行い、直接スキーマだけを変更しない。
- Controllerは調整に留め、ESC/POS通信などの処理はServiceに分離する。
